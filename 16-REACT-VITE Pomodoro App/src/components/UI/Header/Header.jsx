import styled from 'styled-components';
import { StyledH2 } from '../../Headings/Headings';

const StyledHeader = styled.header``;

const Header = () => {
  return (
    <StyledHeader>
      <StyledH2>pomodoro</StyledH2>
    </StyledHeader>
  );
};

export default Header;
